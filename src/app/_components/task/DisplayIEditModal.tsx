// import { getFile } from "@/utils/fileupload";

// async function fetchUploadFile({ id }: { id: string }) {
//   try {
//     const data = await getFile(id);
//   } catch (error) {
//     console.error("Error fetching file:", error);
//   }
// }

// const DisplayImage = async () => {
//   return <div></div>;
// };

// export default DisplayImage;
